# The agentic loop: one provider-neutral loop, message model, and event stream,
# so Vegapunk owns tools/memory/skills/approval and not the plumbing under them.
# Pinned to a commit because it isn't on PyPI (the name there belongs to an
# unrelated logging library) — bump the SHA deliberately, not by re-resolving.
# NOTE: logpose's version string has not moved off 0.1.0, so pip reports this
# requirement "already satisfied" when only the SHA changes. Bumping the pin is
# not enough — reinstall with --force-reinstall, or the old wheel stays.
logpose @ git+https://github.com/xdadwal/logpose@c35fb95
# Still used by vegapunk/embedding.py against Docker Model Runner's /embeddings
# endpoint; the chat path goes through logpose's providers instead.
openai>=1.0
# Subscription-billed Claude backend: official wrapper that bundles the
# Claude Code CLI and speaks to it over a subprocess (auth via `claude /login`
# or CLAUDE_CODE_OAUTH_TOKEN). The raw `anthropic` SDK is API-key-only.
claude-agent-sdk>=0.2
psutil>=5.9
prompt_toolkit>=3.0.52
# Embedded Turso database (import name: turso). Beta — the startup backup in
# vegapunk/db.py exists because of this. The 0.7.1 floor is load-bearing, not
# hygiene: multi-process access (`multiprocess_wal`) only actually works from
# 0.7.1 on, and the scheduler worker depends on it. Note 0.7.1 also regressed
# thread-safety for a shared connection, which vegapunk/db.py's _conn_lock covers.
pyturso>=0.7.1
requests>=2.31
beautifulsoup4>=4.12
