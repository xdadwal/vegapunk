openai>=1.0
# Subscription-billed Claude backend: official wrapper that bundles the
# Claude Code CLI and speaks to it over a subprocess (auth via `claude /login`
# or CLAUDE_CODE_OAUTH_TOKEN). The raw `anthropic` SDK is API-key-only.
claude-agent-sdk>=0.2
psutil>=5.9
prompt_toolkit>=3.0.52
requests>=2.31
beautifulsoup4>=4.12
