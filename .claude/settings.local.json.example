{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Copy to settings.local.json (gitignored). Personal, machine-specific overrides. Merged on top of settings.json; deny rules from any scope always win.",

  "_model_example": "claude-opus-4-8",
  "_permissions_example": {
    "allow": ["Bash(make:*)"],
    "deny": []
  }
}
