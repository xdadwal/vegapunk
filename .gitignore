
# --- Claude Code (managed by ccc) ---
.claude/settings.local.json
.claude/.ccc-source
CLAUDE.local.md

# --- Python ---
.venv/
__pycache__/
*.pyc

# --- Vegapunk local state (REPL history, etc.) ---
.vegapunk/

# --- Vegapunk database (paths are repo-root-relative; the leading / anchors to
#     this .gitignore's directory, not the host filesystem root) ---
/vegapunk.db*
/backups/

# --- Generated / local artifacts (kept out of source; drop scratch files here) ---
/artifacts/
/skills-lock.json
.DS_Store

# --- autoloop scratch (standing prompt) ---
PROMPT.md
