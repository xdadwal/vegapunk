
# --- Claude Code (managed by ccc) ---
.claude/settings.local.json
.claude/.ccc-source
CLAUDE.local.md

# --- Python ---
.venv/
__pycache__/
*.pyc

# --- Vegapunk local state (REPL history, etc.) ---
.vegapunk/

# --- Vegapunk database (sessions, memory, input history) + backups ---
/vegapunk.db
/vegapunk.db-wal
/vegapunk.db-shm
/backups/

# --- autoloop scratch (standing prompt) ---
PROMPT.md
